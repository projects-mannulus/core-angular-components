import { StepperContentDirective } from "./stepper-content.directive";

describe("StepperContentDirective", () => {
  it("should create an instance", () => {
    const directive = new StepperContentDirective(null);
    expect(directive).toBeTruthy();
  });
});
